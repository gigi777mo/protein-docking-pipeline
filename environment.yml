name: docking
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.11
  - openbabel
  - rdkit
  - biopython
  - pandas
  - numpy
  - pip
  - pip:
    - vina          # official Python bindings (optional but useful)
